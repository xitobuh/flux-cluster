kind: Secret
proxmox_api_url: ${BOOTSTRAP_PROXMOX_API_URL}
proxmox_api_token_id: ${BOOTSTRAP_PROXMOX_API_ID}
proxmox_api_token_secret: ${BOOTSTRAP_PROXMOX_API_SECRET}
proxmox_node_names: ${BOOTSTRAP_PROXMOX_NODE_NAMES}
proxmox_vms: ${BOOTSTRAP_PROXMOX_VMS}
proxmox_vm_start_id: ${BOOTSTRAP_PROXMOX_VM_START_ID}
proxmox_vm_template_name: ${BOOTSTRAP_PROXMOX_VM_TEMPLATE_NAME}
proxmox_vm_cores: ${BOOTSTRAP_PROXMOX_VM_CORES}
proxmox_vm_sockets: ${BOOTSTRAP_PROXMOX_VM_SOCKETS}
proxmox_vm_memory: ${BOOTSTRAP_PROXMOX_VM_MEMORY}
proxmox_vm_disk_size: ${BOOTSTRAP_PROXMOX_VM_DISK_SIZE}
proxmox_vm_storage: ${BOOTSTRAP_PROXMOX_VM_STORAGE}
proxmox_vm_network_bridge: ${BOOTSTRAP_PROXMOX_VM_NETWORK_BRIDGE}
proxmox_vm_ip_start: ${BOOTSTRAP_PROXMOX_VM_IP_START}
proxmox_vm_ip_gw: ${BOOTSTRAP_PROXMOX_VM_IP_GW}
proxmox_vm_ip_dns: ${BOOTSTRAP_PROXMOX_VM_IP_DNS}
proxmox_vm_searchdomain: ${BOOTSTRAP_PROXMOX_VM_SEARCHDOMAIN}
proxmox_vm_user: ${BOOTSTRAP_PROXMOX_VM_USER}
proxmox_vm_password: ${BOOTSTRAP_PROXMOX_VM_PASSWORD}
proxmox_vm_ssh_pubkey: ${BOOTSTRAP_PROXMOX_VM_SSH_PUBKEY}
